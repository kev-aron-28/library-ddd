<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Resultados de Búsqueda</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f4f4f9;
            margin: 0;
            padding: 0;
        }

        .container {
            max-width: 900px;
            margin: 40px auto;
            background-color: #ffffff;
            padding: 30px;
            border-radius: 12px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
        }

        h1 {
            font-size: 28px;
            margin-bottom: 30px;
            text-align: center;
            color: #333;
            font-weight: 600;
        }

        ul {
            list-style-type: none;
            padding: 0;
        }

        li {
            margin-bottom: 20px;
        }

        .book-item {
            padding: 20px;
            border: 1px solid #e0e0e0;
            border-radius: 8px;
            background-color: #fafafa;
            transition: background-color 0.3s, box-shadow 0.3s, transform 0.3s;
            position: relative;
            overflow: hidden;
        }

        .book-item h3 {
            margin: 0;
            font-size: 22px;
            color: #0073e6;
        }

        .book-item p {
            margin: 10px 0;
            color: #555;
        }

        .book-item a {
            text-decoration: none;
            color: inherit;
            display: block;
        }

        .book-item a:hover {
            background-color: #f0f4ff;
            box-shadow: 0 6px 15px rgba(0, 0, 0, 0.1);
            transform: translateY(-5px);
        }

        .book-item::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 4px;
            height: 100%;
            background-color: #0073e6;
            transition: width 0.3s;
        }

        .book-item a:hover::before {
            width: 100%;
        }

        .no-results {
            text-align: center;
            font-size: 20px;
            color: #888;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Resultados de Búsqueda</h1>
        <% if (books.length > 0) { %>
            <ul>
                <% books.forEach(book => { %>
                    <li class="book-item">
                        <a href="/backoffice/books/<%= book.id %>/detail">
                            <h3><%= book.title %></h3>
                            <p><strong>Autor:</strong> <%= book.author.name %></p>
                            <p><strong>ISBN:</strong> <%= book.isbn %></p>
                            <p><strong>Categorías:</strong> <%= book.categories.map(category => category.name).join(', ') %></p>
                            <p><strong>Copias:</strong> <%= book.copies.length %></p>
                        </a>
                    </li>
                <% }); %>
            </ul>
        <% } else { %>
            <p class="no-results">No se encontraron libros con el criterio proporcionado.</p>
        <% } %>
    </div>
</body>
</html>
